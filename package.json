{
  "name": "brain-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Agent plugin: a brain/ folder, read first and kept current.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-cursor-rule.mjs",
    "test": "node hooks/project.test.mjs && node hooks/stop.test.mjs && node scripts/sync-cursor-rule.mjs --check"
  }
}
